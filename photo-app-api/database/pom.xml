<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.ossantvng.photoapp</groupId>
    <artifactId>photo-app-database</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>photo-app-database</name>
    <description>Photo App Database</description>

    <properties>
        <java.version>25</java.version>
        <db.host>localhost</db.host>
        <db.port>3306</db.port>
        <db.name>photo_app</db.name>
        <db.username>photo_app_user</db.username>
        <db.password>password</db.password>
    </properties>

    <dependencies>
        <!-- Liquibase -->
        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
            <version>4.29.2</version>
        </dependency>

        <!-- MySQL Driver -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <version>9.7.0</version>
            <scope>compile</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.liquibase</groupId>
                <artifactId>liquibase-maven-plugin</artifactId>
                <version>4.29.2</version>
                <configuration>
                    <searchPath>src/main/resources</searchPath>
                    <changeLogFile>db/changelog/changelog-master.xml</changeLogFile>
                    <username>${db.username}</username>
                    <password>${db.password}</password>
                    <url>jdbc:mysql://${db.host}:${db.port}/${db.name}?allowPublicKeyRetrieval=true&amp;useSSL=false</url>
                    <driver>com.mysql.cj.jdbc.Driver</driver>
                    <promptOnNonLocalDatabase>false</promptOnNonLocalDatabase>
                </configuration>
                <executions>
                    <execution>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>update</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
