<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.0.xsd">

    <changeSet id="11" author="ossantvng">
        <sql>
            INSERT INTO albums (account_id, title, description, active_album, created_at, updated_at)
            SELECT a.id,
                   CONCAT('album_', a.id, '_1'),
                   CONCAT('Álbum de la cuenta ', a.account_name, ' número 1'),
                   TRUE,
                   CURRENT_TIMESTAMP,
                   CURRENT_TIMESTAMP
            FROM accounts a;

            INSERT INTO albums (account_id, title, description, active_album, created_at, updated_at)
            SELECT a.id,
                   CONCAT('album_', a.id, '_2'),
                   CONCAT('Álbum de la cuenta ', a.account_name, ' número 2'),
                   TRUE,
                   CURRENT_TIMESTAMP,
                   CURRENT_TIMESTAMP
            FROM accounts a;

            INSERT INTO albums (account_id, title, description, active_album, created_at, updated_at)
            SELECT a.id,
                   CONCAT('album_', a.id, '_3'),
                   CONCAT('Álbum de la cuenta ', a.account_name, ' número 3'),
                   TRUE,
                   CURRENT_TIMESTAMP,
                   CURRENT_TIMESTAMP
            FROM accounts a;
        </sql>
    </changeSet>

</databaseChangeLog>
