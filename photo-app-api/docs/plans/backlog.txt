BACKLOG — findings deferred out of the step that found them
===========================================================

Running list of things worth fixing that were out of scope when discovered.
Append here rather than only mentioning them in a summary, so nothing is lost
between sessions. Format: [YYYY-MM-DD | Step N] title, then detail.


[2026-08-01 | reported] HIGH — 500 for authenticated non-admin users on protected
endpoints                                                   [CONFIRMED AND FIXED]
------------------------------------------------------------------------------------
Reported by Oscar, reproduced, diagnosed and fixed on 2026-08-01.

SYMPTOM: a user who authenticated successfully but lacked the required role got
HTTP 500 "Unexpected error occurred" on an @PreAuthorize-protected endpoint,
instead of 403.

ROOT CAUSE: @PreAuthorize denial throws
org.springframework.security.authorization.AuthorizationDeniedException, which
extends AccessDeniedException extends RuntimeException. It is thrown by
AuthorizationManagerBeforeMethodInterceptor INSIDE the DispatcherServlet, so
GlobalExceptionHandler's @ExceptionHandler(Exception.class) catch-all matched it
first and returned 500. Because the advice handled it, the exception never
propagated out to Spring Security's ExceptionTranslationFilter — the component
that would route AccessDeniedException to the configured accessDeniedHandler.
That accessDeniedHandler in SecurityConfiguration was therefore dead code for
method-security denials; it only ever fired for filter-level
authorizeHttpRequests denials.

NOT A REGRESSION FROM STEPS 1-6. Verified: GlobalExceptionHandler is untouched by
every commit in this session (git log 751d250..HEAD for that file is empty); the
catch-all predates the work (commits 8a81452, e1e98eb); and @EnableMethodSecurity,
accessDeniedHandler and all eight @PreAuthorize annotations already existed at
751d250, the commit before Step 1. This was a pre-existing latent bug, live for as
long as @PreAuthorize and the catch-all had coexisted. It had simply never been
exercised, because every test token used before this point carried scope
["USER","ADMIN"] and was therefore always authorised.

RULED OUT during diagnosis:
  - The Step 1 /error permitAll change is NOT involved. The 500 body carried the
    ApiErrorDTO shape (httpStatus/error/message/path/timeStamp) produced by
    GlobalExceptionHandler.buildResponse, meaning the advice returned a
    ResponseEntity directly and no servlet ERROR dispatch occurred. An ERROR
    dispatch would have produced Spring's default {timestamp,status,error,path}.
  - CustomFeignErrorDecoder is NOT involved. It appeared only in the ADMIN
    request's trace, correctly decoding a genuine downstream 503. A USER-only
    request never reaches Feign; @PreAuthorize rejects it before the controller
    body runs.

FIX: added @ExceptionHandler(AccessDeniedException.class) to GlobalExceptionHandler,
positioned ABOVE the catch-all, returning 403 with the standard ApiErrorDTO body and
logging at WARN (an authorization denial is an expected outcome, not a fault).
Catching AccessDeniedException also covers AuthorizationDeniedException. Required
adding spring-security-core to photo-app-commons — deliberately the core artifact,
not the starter, consistent with the earlier narrowing of that module.

BLAST RADIUS — one fix covers everything. There is exactly ONE @RestControllerAdvice
in the entire codebase, in photo-app-commons. All five services resolve commons
(authorization-service transitively via photo-app-feign-lib) and all five
component-scan com.photoapp.commons. No per-service duplication exists.

VERIFIED after the fix, on two different services and two different endpoints:
  accounts POST /accounts            no token 401 | USER-only 403 | USER+ADMIN 503*
  albums   DELETE /albums/byAccountIds no token 401 | USER-only 403 | USER+ADMIN 404*
  (* the admin results are correct pass-through: a downstream service was not
     running, and account 999 does not exist)
  403 body: {"httpStatus":403,"error":"Forbidden","message":"Forbidden","path":...}
  Denials logged as ACCESS_DENIED at WARN; UNHANDLED_EXCEPTION count zero.


[2026-08-01 | Step 8] No traces reach Zipkin from containers        [FIXED]
---------------------------------------------------------------------------
FIXED 2026-08-01 by replacing the span transport. Boot 4 sends spans through
ZipkinHttpClientSender, built on the JDK java.net.http.HttpClient, which fails
permanently in containers (see root cause below). photo-app-tracing-lib now
publishes a BytesMessageSender backed by URLConnectionSender; Boot declares its
own sender under @ConditionalOnMissingBean(BytesMessageSender.class), so it
backs off. HttpURLConnection was already proven to work in these exact
containers during diagnosis.

The library is registered as an auto-configuration ordered before
ZipkinAutoConfiguration, not as a @Component, because the Config Server and the
Discovery Service do not component-scan any com.photoapp package. It is a new
module because no existing library is shared by all eight deployables - those
two depend on none of them.

The underlying trigger that breaks the JDK client is NOT fixed, only bypassed.
It is tracked as a separate follow-up item below.

Original diagnosis retained:
--------------------------------------------------------------------
Tracing worked in the Step 3 NATIVE run (all eight components reported spans).
In containers it does not: Zipkin receives nothing from any application service.
Everything else in Step 8 verified fine, so this is isolated to span delivery.

WHAT IS RULED OUT (all measured, not assumed):
  - Configuration is correct. /actuator/env with show-values enabled reports
    management.zipkin.tracing.endpoint = http://photo-app-zipkin:9411/api/v2/spans
    sourced from systemEnvironment (origin: MANAGEMENT_ZIPKIN_TRACING_ENDPOINT),
    and management.tracing.sampling.probability = 1.0 from the config server.
  - Spans ARE being created and sampled: the service logs
    "Dropped 37 spans due to ConnectException()" - it has spans to send.
  - The collector is healthy and reachable FROM THE SERVICE CONTAINER:
      curl http://photo-app-zipkin:9411/health            -> 200
      curl -X POST .../api/v2/spans with a hand-written span -> 202,
    and that manual span IS stored and queryable (service "manual-probe-svc"
    appears in /api/v2/services). So DNS, routing and the collector all work.
  - Not a restart-ordering artifact: restarting a single service while Zipkin had
    been stable for minutes still produced 9 drops and no stored spans.
  - Tracing auto-configuration is active: braveTracer and zipkinConnectionDetails
    beans exist; BraveAutoConfiguration, ZipkinAutoConfiguration and
    ZipkinWithBraveTracingAutoConfiguration are all present.

ROOT CAUSE (found 2026-08-01, second investigation pass)
The failure is in the HTTP transport, not in configuration or networking. Full
stack trace from the reporter:

  java.net.ConnectException
    at jdk.internal.net.http.HttpClientImpl.send
    at org.springframework.boot.zipkin.autoconfigure.ZipkinHttpClientSender.postSpans
    at zipkin2.reporter.internal.AsyncReporter$BoundedAsyncReporter.flush
  Caused by: java.net.ConnectException
    at jdk.internal.net.http.PlainHttpConnection.connectAsync
  Caused by: java.nio.channels.ClosedChannelException
    at sun.nio.ch.SocketChannelImpl.ensureOpen
    at sun.nio.ch.SocketChannelImpl.connect

Boot 4 sends spans through ZipkinHttpClientSender, which uses the JDK
java.net.http.HttpClient. The socket channel is ALREADY CLOSED when connect is
attempted - ensureOpen fails before any network I/O happens. That is why the
address is irrelevant and why nothing outside the JVM can observe a failure.

BOTH SUGGESTED LEADS WERE TESTED AND RULED OUT:
  1. IPv4/IPv6 preference - NOT the cause. A probe inside the service container
     shows java.net.preferIPv4Stack unset, "photo-app-zipkin" resolving to a
     single IPv4 address (172.19.0.2, ipv6=false), a successful TCP connect to
     172.19.0.2:9411, and HttpURLConnection returning 200. Adding
     -Djava.net.preferIPv4Stack=true would change nothing.
  2. Property/bean binding timing - NOT the cause. /actuator/env with
     show-values reports the endpoint correctly from systemEnvironment, and the
     sender demonstrably uses it: pointing it at a dead port (19411) fails the
     same way, and pointing it at the raw IP instead of the hostname also fails
     the same way. The value is right and it is being used.

DECISIVE CONTROL: a freshly constructed java.net.http.HttpClient, in the same
container, on a clean thread, POSTing to the same URL returns HTTP 202. The JDK
client, the network and the collector are all fine - only the long-lived client
inside the running application is broken.

WHERE TO GO NEXT:
  - ClosedChannelException on a fresh channel is the classic symptom of the
    calling thread carrying an interrupt flag: NIO auto-closes interruptible
    channels on interrupt. Find what interrupts AsyncReporter$Flusher, or what
    breaks the HttpClient's selector state. Note the app is otherwise healthy,
    so this is confined to the reporter's thread/client.
  - Practical workaround if the root trigger proves elusive: replace the
    transport. Boot lets a custom BytesMessageSender bean override
    ZipkinHttpClientSender - zipkin-sender-urlconnection or the OkHttp sender
    avoids the JDK HttpClient entirely, and HttpURLConnection is already proven
    to work in this exact container.
  - Worth checking whether Step 3's native run used the same sender; if it did,
    the trigger is environmental rather than a code difference.
Priority: HIGH - observability is a stated goal of the containerised stack, and
everything except span delivery is verified working.


[2026-08-02 | config] MEDIUM — review logging levels across the config repo
----------------------------------------------------------------------------
The Hibernate fix applied earlier today addressed one logger. This is the
systematic pass.

PRINCIPLE
Local and dev can stay reasonably verbose - small volume, active debugging. What
is missing is a defined production posture: INFO as the default floor for most
packages, with DEBUG and TRACE reserved for specific loggers switched on
temporarily during an investigation rather than left on permanently.

AUDIT OF WHAT IS ACTUALLY SET (2026-08-02), so the work starts from facts:

  Still at TRACE in every dev profile - the loudest remaining, and considerably
  noisier than the Hibernate binder that was just lowered:
      org.springframework.cloud.openfeign=TRACE       (5 files)
      org.springframework.cloud.circuitbreaker=TRACE  (5 files)
      io.github.resilience4j=TRACE                    (5 files)
  These fire on every inter-service call, so they scale with traffic rather than
  with data volume.

  Application packages at DEBUG in dev, one per service:
      com.photoapp.{users,accounts,albums,photos,auth}=DEBUG

  Gateway: org.springframework.cloud.gateway is DEBUG in dev, INFO in prod, plus
  ...gateway.server.mvc.handler=TRACE in dev.

GOOD NEWS - THE DEV/PROD SPLIT PARTLY EXISTS ALREADY
The prod profiles are not simply copies. They already lower the Hibernate loggers
to info while dev runs them hotter, so the pattern this item asks for is
established; it just has not been applied to the Feign, circuit-breaker and
resilience4j loggers, which prod does not mention at all and therefore inherits
at the root level. Extending the existing pattern is the job, not inventing one.

CONCRETE LEFTOVER WORTH FIXING FIRST
logging.level.org.hibernate.type.descriptor.sql.BasicBinder still appears in the
PROD files (4 at info, 1 at trace elsewhere). That logger is DEAD on Hibernate
7.2.7 - the class moved to org.hibernate.type.descriptor.jdbc, verified against
hibernate-core-7.2.7.Final.jar during the earlier fix. It was deleted from the dev
profiles but survives in prod. Delete it there too; it can never emit.

SCOPE
Audit every logging.level.* entry across all service properties, group by package,
and document the intended level per environment in a new LOGGING.md.
NOTE: DATABASE.md does not exist in this repository - LOGGING.md as a new file is
the cleaner home, and Step 9 documentation is the natural moment to add it.

CONNECTION TO THE LATENCY ITEM
Logging volume was measured and ruled out as the dominant cause for /accounts
specifically - that endpoint produced only two binder lines per request. The
broader principle still holds: verbose logging has real cost at scale, and the
Feign and resilience4j TRACE loggers above are per-call rather than per-row, so
they will scale with production traffic in a way the Hibernate binder did not with
one developer's manual testing.

Priority: MEDIUM.


[2026-08-02 | initiative] HIGH — comprehensive unit and integration testing
----------------------------------------------------------------------------
Deferred throughout the whole dockerization effort (Steps 1-9) by deliberate
decision, not oversight. Promoted here to its own item rather than living as an
out-of-scope note inside dockerization-plan.md.

Spring Boot project, on WebMVC since Step 1: use @SpringBootTest and @WebMvcTest
with MockMvc. Not Micronaut tooling.

GOAL
Catch design-level errors that only surfaced under real usage - exactly like the
authorization defect found while investigating after Step 8, where a ROLE_USER
token on an ADMIN endpoint returned 500 instead of 403 because @PreAuthorize's
AuthorizationDeniedException was swallowed by GlobalExceptionHandler's
@ExceptionHandler(Exception.class) catch-all. Worth stressing: that bug was
PRE-EXISTING, not introduced by any step, and it survived undetected only because
every manual test token happened to carry ADMIN. Write tests for the whole CLASS
of defect, not the single instance.

COVERAGE SCOPE - five business services plus the gateway
  1. Happy path. CRUD per controller: create, read, update, delete, list with
     pagination. Assert status codes AND response shape.
     NOTE - correction to the original framing: there is no PagedResponseDTO in
     this codebase. Listing endpoints return Spring's Page<T>, and commons holds
     only PaginationInputDTO, SortInputDTO and PaginationUtil on the input side.
     Serialising Page<T> over JSON is itself historically fragile, so assert the
     concrete serialised shape rather than assuming it is stable.
  2. Authorization matrix. For EVERY @PreAuthorize endpoint: no token -> 401,
     wrong role -> 403, correct role -> reaches business logic. Systematically,
     across all endpoints, not only those already known to be protected. This is
     the Step 8 bug class.
  3. Exception handling. Every branch of GlobalExceptionHandler:
     MethodArgumentNotValidException, ConstraintViolationException, not-found,
     OptimisticLockException, DataAccessException, AccessDeniedException (the
     Step 8 fix) and the catch-all. Assert the exact status and ApiErrorDTO shape,
     not merely that a response came back.
  4. Feign behaviour. Four clients exist: AccountFeignClient, AlbumFeignClient,
     PhotoFeignClient, UserFeignClient. Known live paths include accounts ->
     users (UserFeignClient#isActive) and albums -> accounts
     (AccountFeignClient#findById) - both observed during Step 8. For each: the
     success path, a downstream 4xx decoded by CustomFeignErrorDecoder, and a
     downstream 5xx or unavailable producing the correct fallback. The fallbacks
     already exist and are worth asserting directly - isActiveFallback,
     findByUsernameAndActiveUserFallback, findAllFallback.
  5. Resilience4j. Circuit breaker transitions closed -> open -> half-open, retry
     behaviour, and fallbacks returning the expected shape and status rather than
     leaking a raw exception.
  6. Repository and persistence. For non-trivial query logic - filters,
     pagination, ownership-scoped queries - test against Testcontainers with
     MySQL rather than H2, so dialect differences cannot mask real bugs.
  7. MapStruct mappers. Step 5 proved ModelMapper had a silent field-name
     mismatch that returned null to the API for months. ReportingPolicy.ERROR now
     catches that at compile time, so mapper tests are the runtime safety net on
     top of the compile-time guarantee, not a substitute for it.

NOT YET DECIDED
  - coverage targets and thresholds
  - whether the four library modules get their own suites or are covered only
    indirectly through the services consuming them
  - whether to add mutation testing, e.g. PIT, to check the tests are meaningful
    rather than merely present

Priority: HIGH conceptually. The Step 8 defect is concrete proof this class of
bug is real and currently undetected everywhere else in the codebase.
Deliberately sequenced AFTER Step 9 documentation, as its own initiative rather
than squeezed into dockerization scope.


[2026-08-02 | platform decision] Use an OIDC provider for real projects, but design
against the protocol rather than the vendor
-----------------------------------------------------------------------------------
STRATEGIC / LONG-TERM. Not a defect, and NOT a plan to migrate
photo-app-authorization-service. That service stays exactly as it is: its value is
that it was built from scratch as a learning exercise. This records the decision
for the NEXT project and for the generic security library, so the reasoning is not
lost. Closely related to the PKCE item below.

DECISION (updated 2026-08-02): AWS COGNITO
The provider is AWS Cognito, deployed alongside ECS Fargate and intended to carry
through to PRODUCTION, not just local development. Chosen for this project and as
the default going forward, pending future business-model clarity.

Application code is still written against the OIDC/OAuth2 standard rather than
against Cognito. That principle is unchanged by the choice, for two reasons:

  1. Cognito's OIDC compliance has AWS-specific quirks worth insulating against.
     Ones to verify at implementation time, since Cognito's behaviour has shifted
     over the years:
       - access tokens historically carry no "aud" claim, using client_id instead,
         which breaks Spring's default audience validation. ID tokens do carry aud.
       - group membership arrives as "cognito:groups", not a standard claim, so the
         authorities mapping needs an explicit claim name.
       - the logout endpoint is /logout rather than a standard end_session_endpoint.
       - issuer-uri takes the form
         https://cognito-idp.{region}.amazonaws.com/{userPoolId}
     Each of these is a configuration or converter concern, and none should leak
     into the library.
  2. It keeps per-client provider swaps open. If an enterprise client later requires
     on-prem Keycloak, that becomes a configuration change rather than a rewrite.

The original decision text follows, and still holds - only the provider is now
settled.

ORIGINAL FRAMING
Adopt Keycloak - or any OIDC-compliant provider - as the default identity provider
for future projects handling real customer data, while writing all application code
against the OIDC/OAuth2 standard rather than against a specific provider.

WHY NOT KEEP THE CUSTOM IMPLEMENTATION
Authentication is one of the few areas where a subtle implementation error becomes
a direct security breach rather than a bug. Keycloak, Okta and Auth0 have absorbed
years of dedicated security review; a self-built issuer has not. The learning value
is real, the production risk is not worth taking.

THE MECHANISM FOR STAYING PROVIDER-AGNOSTIC
Every OIDC-compliant provider - Keycloak, Okta, Auth0, or a future custom server
built on Spring Authorization Server - exposes a standard issuer-uri and a JWKS
endpoint for public-key verification. Spring Security's oauth2-resource-server
consumes that natively:

    spring:
      security:
        oauth2:
          resourceserver:
            jwt:
              issuer-uri: https://your-provider.com/realms/your-app

From that one property Spring discovers the JWKS endpoint, fetches and caches the
public keys, and validates signature, expiry and issuer on every request. No custom
JWT parsing code at all.

WHY THIS IS A DIFFERENT SHAPE FROM WHAT EXISTS TODAY
The current setup signs with HS256 using a symmetric secret, so every service needs
the SAME shared secret in order to verify - which is also why photoapp.jwt.secret
is duplicated across seven config files today, and why decision 7 in the
dockerization plan kept that arrangement for the current round. Changing providers
under that model means replacing the mechanism outright.

With OIDC and JWKS the keys are asymmetric: only the identity provider holds the
private signing key, and each microservice needs nothing but the public key, fetched
automatically from the issuer-uri. Swapping providers becomes a configuration change
- the issuer-uri value, and possibly a claim name for roles - instead of a rewrite.
It also removes the current property that every service can MINT tokens, not merely
verify them.

IMPLICATION FOR THE FUTURE SECURITY LIBRARY (see PLATFORM-VISION.md)
Build it on Spring's standard JwtAuthenticationConverter and
JwtGrantedAuthoritiesConverter, mapping standard JWT claims onto CustomUserPrincipal.
No Keycloak-specific code anywhere inside the library. Only the issuer-uri and the
claim-name mappings are provider-specific, and those stay configuration - swappable
per environment or per client without touching how the services integrate with the
library.

NOT YET DECIDED - delivery and hosting model
Whether future client work runs in an own AWS account or in the client's own
infrastructure is an open BUSINESS question, deliberately out of scope for this
project's technical decisions. It is recorded here only because it is the thing
most likely to force a per-client provider swap, which is precisely what the
protocol-not-vendor principle above is there to absorb. Do not let it block
technical work; the design already tolerates either outcome.

Priority: strategic. Revisit when starting the next real project, or when
generalising photo-app-security-lib into the toolkit.

Note on the AWS backlog section of dockerization-plan.md: it lists "true OAuth2
PKCE flow" and "RSA/JWK signing with verify-only resource servers" as production
items. Both are subsumed by this decision - Cognito provides the authorization
server and the JWKS endpoint, so those entries should be read as "integrate with
Cognito", not as "build it".


[2026-08-02 | observation] PKCE flow is not a genuine PKCE implementation
--------------------------------------------------------------------------
KNOWN SIMPLIFICATION, NOT A BUG. PLATFORM-VISION.md already says the
authorization service "registers an in-memory PKCE client; it is a stepping
stone, not the product". Logged so it is not forgotten.

/auth/login accepts a codeVerifier directly in the login request body. That is a
JWT login with an extra field, not the PKCE authorization code flow, which
requires a client-generated code_verifier and derived code_challenge, a redirect
to an authorization endpoint, a temporary authorization code, and an exchange of
that code for a token using the original verifier.

VERIFIED IN THE CODE (2026-08-02), and it is thinner than the description above
suggests:
  - LoginRequestDTO declares codeVerifier with
    @NotBlank("Code verifier is required for PKCE"), so callers are FORCED to
    send one.
  - Nothing consumes it. Grepping the whole service for "odeVerifier" outside
    that DTO returns no hits. The value is required, then discarded - it is not
    hashed, compared, or stored. So the field is validation theatre rather than
    a partial implementation.
  - The service exposes /auth/login, /auth/refresh and /auth/revoke. There is no
    authorization endpoint at all, so no authorization code can ever be issued.
  - AuthorizationServerConfig registers a client with grant type
    AUTHORIZATION_CODE and redirect URI http://localhost:8080/callback, but
    nothing in the login path uses that RegisteredClientRepository. The config is
    inert. The redirect URI also points at localhost, which would not resolve
    correctly from inside a container.

CORRECT FIX: build the real authorization server on Spring Authorization Server,
per the platform vision. Do NOT patch the custom endpoint into something
PKCE-shaped piecemeal - that would entrench the stepping stone.

Small cleanup worth considering in the meantime, independent of that initiative:
either drop the unused codeVerifier field, or document it in place as a
placeholder. As it stands it requires every caller to send a value that is
ignored, which reads as a working PKCE check to anyone new to the code.

Priority: aligned with the future authorization-server initiative, not urgent for
the dockerization scope. See PLATFORM-VISION.md.


[2026-08-01 | Step 8] MEDIUM — endpoints are slow through the gateway in Docker
--------------------------------------------------------------------------------
Not blocking; the stack is functionally correct. Observed in Zipkin traces once
tracing started working:

    POST /auth/login    ~7.0s total at the gateway
                        ~6.3s of that inside photo-app-authorization-service
    GET  /users         3.2s total, listing 100 records
    GET  /accounts      4.828s total, 17 spans across 3 services
                        traceId 6a6ea8fed78d9418f5aff2b0ec6c6fd4

Far higher than expected for a local environment at this data volume. BCrypt's
deliberate cost factor could account for part of the login figure, but the
/users listing has no such excuse - it is a simple paginated read.

The /accounts trace confirms the problem is NOT confined to one service. Three
services and 17 spans for one request means the cost compounds per hop, so a
systemic per-hop overhead fits the evidence better than one slow component. That
strengthens starting point 1 below: resource contention would degrade every hop
at once, which is exactly the shape observed.

The fan-out is via Feign and is already identified from earlier work in this
session: accounts-service calls users-service through
UserFeignClient#isActive - visible in a Step 8 stack trace as
"Failure on isActive. The photo-app-users-service is not available" when
users-service was down. The third service in the trace is the gateway. So the
path is gateway -> accounts-service -> users-service.

Additional angle worth checking: whether any Feign calls in the accounts flow
are sequential where they could run in parallel. Look at AccountServiceImpl and
the Feign clients in photo-app-feign-lib. Note this is a code-shaped fix and the
latency may well be environmental - measure before restructuring anything,
otherwise it is optimising the wrong layer.

LIKELY ROOT CAUSE IDENTIFIED - Hibernate TRACE logging (evidence, not hypothesis)
In trace 6a6ea9c1ae30762e70012572db19d123 (albums-service) there is a ~1.8s gap
between "JWT validated successfully" at 02:21:54.842 and the first Hibernate
"binding parameter" line at 02:21:56.664, for a query that should take under
100ms. TRACE-level Hibernate logging emits one line per bound parameter per
query; every line is serialised to JSON and written to the shared log volume,
which plausibly accounts for multi-second latencies on any endpoint that touches
the database.

CONFIRMED CONFIGURATION - identical in all five service *-dev.properties:
    spring.jpa.show-sql=true
    spring.jpa.properties.hibernate.format_sql=true
    logging.level.org.hibernate.SQL=debug
    logging.level.org.hibernate.orm.jdbc.bind=trace
    logging.level.org.hibernate.type.descriptor.sql.BasicBinder=trace
So the TRACE levels are explicit, not inherited defaults. Three observations:
  - show-sql=true duplicates what org.hibernate.SQL=debug already logs; one of
    the two is redundant.
  - format_sql=true pretty-prints each statement across multiple lines, which
    multiplies the volume written per query on top of the per-parameter lines.
  - org.hibernate.type.descriptor.sql.BasicBinder is the Hibernate 5 binder
    logger. This project runs Hibernate 7.2.7, where it was replaced by
    org.hibernate.orm.jdbc.bind - which is also set. The BasicBinder line is
    therefore almost certainly dead config; verify, then delete it rather than
    just lowering it.

FIX APPLIED 2026-08-01 (config repo 719e5ee) - BUT IT DID NOT EXPLAIN THE NUMBERS
All four changes were made and verified live: org.hibernate.orm.jdbc.bind is now
WARN, show-sql and format_sql are gone, and the dead BasicBinder line is deleted.
org.hibernate.SQL stays at DEBUG and still works - a real request logs 2 SQL
statements, confirmed after the change.

The BasicBinder deletion was verified before removal, not assumed:
org/hibernate/type/descriptor/sql/BasicBinder.class is ABSENT from
hibernate-core-7.2.7.Final.jar. The class moved to
org.hibernate.type.descriptor.jdbc, so nothing could ever have logged under the
configured name.

MEASURED EFFECT - much smaller than the hypothesis predicted. A/B on the same
container, toggling the binder logger through the actuator, interleaved twice to
control for drift, 10 requests each, every response verified 200:

    bind=TRACE   avg 0.057s / 0.048s
    bind=WARN    avg 0.053s / 0.039s

A ~10-20% difference, inside the min/max spread - not a multi-second win. Log
volume tells the same story: 3 requests to /accounts produced 24 lines at TRACE
versus 18 at WARN, i.e. only 2 binder lines per request. That endpoint binds few
parameters (pagination limit and offset), so there was little noise to remove.
The hypothesis should hold better on endpoints that bind many parameters or that
issue N+1 queries - it was simply the wrong endpoint to test it against.

THE 4.828s FIGURE COULD NOT BE REPRODUCED AT ALL
GET /accounts in steady state runs at roughly 30-60ms, cold or warm, before and
after the change. The original 4.828s, 3.2s and 7s figures were almost certainly
FIRST-REQUEST-AFTER-STACK-START measurements, which fold in JIT warm-up, Eureka
registration propagation, gateway route and load-balancer resolution, Feign
client initialisation and connection-pool fill. That reframes the whole item:
there may be no steady-state latency problem, only an expensive first request.

COLD-START BOOT TIMES MEASURED 2026-08-02 - the hypothesis above, now with numbers
Gathered incidentally while fixing an unrelated problem (healthcheck start-period,
90s -> 240s; see the separate note at the end of this section). These are DIRECTLY
MEASURED container boot times, not latency inferred from request timings, which
makes them a much stronger data point than the 4.828s/3.2s/7s figures above.

Source: Spring's own "Started ...Application in X seconds (process running for Y)"
line, Y quoted below. Scenario: a genuine `docker compose down -v` followed by a
full `stack.sh up` - every volume destroyed, empty page cache, six JVMs
initialising simultaneously, 12GB WSL2 engine.

  Run 1                      Run 2 (repeat, same conditions)
    users           171.7s     users           168.0s
    accounts        170.8s     accounts        165.4s
    photos          168.6s     photos          165.7s
    albums          164.4s     albums          172.9s
    authorization   162.7s     authorization   172.9s
    gateway         110.9s     gateway         107.9s
    config-server    25.1s
    discovery        18.7s

Two independent runs, so this is reproducible rather than a one-off. Spread across
runs is ~8s per service; the ordering of the five business services shuffles
between runs, which is itself consistent with contention rather than with any one
service being intrinsically slow.

RUN 3 - 2026-08-05, CPU ALLOCATION RAISED 6 -> 10. HYPOTHESIS CONFIRMED.
The experiment proposed below was run: .wslconfig CPUs raised to 10 (docker info
NCPU=10, host has 12 logical), then the identical cold start - docker compose
down -v followed by stack.sh up, all volumes destroyed, same extraction method.

  component        run1(6cpu)  run2(6cpu)  run3(10cpu)   mean(6)   change
  users               171.7       168.0        93.7       169.8    -44.8%
  accounts            170.8       165.4        91.7       168.1    -45.4%
  photos              168.6       165.7        93.0       167.1    -44.3%
  albums              164.4       172.9        91.9       168.7    -45.5%
  authorization       162.7       172.9        88.9       167.8    -47.0%
  gateway             110.9       107.9        68.7       109.4    -37.2%
  config-server        25.1          -         18.4        25.1    -26.7%
  discovery            18.7          -         18.9        18.7     +1.1%

THE CONTROL CASE IS WHAT SETTLES IT
discovery-service boots ALONE, after config-server is healthy and before anything
else starts, so it was never contended. It is UNCHANGED: 18.9s against 18.7s,
+1.1%, pure noise. Every component that boots simultaneously with five others
dropped 37-47%.

That is the signature CPU contention predicts and that nothing else explains. A
slow disk, a cold page cache, or slow class loading would have sped up the
uncontended component too. Only contention leaves the lone booter untouched while
halving the time of the crowd.

The magnitude agrees as well. Cores per simultaneously-booting JVM went from
6/6 = 1.00 to 10/6 = 1.67, +67%. If boot time scaled linearly with cores per JVM
the prediction is 60% of the previous time, i.e. -40%. Measured -45%. Slightly
better than linear, which is what you expect when the extra headroom also relieves
the infrastructure containers competing for the same cores.

WHAT IT DOES NOT SAY
Contention was the DOMINANT factor, not the only one. Six JVMs still take ~90s to
boot with 1.67 cores each, which is not fast; there is a remaining component that
more cores did not remove. Whoever picks this up next should treat ~90s as the new
baseline rather than assuming the problem is closed.

Also note the healthcheck start-period, set to 240s on 2026-08-02 to clear a
172.9s worst case, now has far more headroom than intended - the slowest component
is 93.7s. It can safely stay where it is; the only cost is slower reporting of a
genuinely hung service. If it is ever retuned, retune it against these numbers and
not the old ones.

WHAT THIS CONFIRMS
The five business services and the gateway take 108-173s to boot under this exact
contention scenario, while config-server and discovery are up in under 30s. That
is a 5-7x difference between components running the same Spring Boot startup
machinery, differing mainly in that the six slow ones start AT THE SAME TIME as
each other while the two fast ones start alone, earlier in the dependency chain.

That is the shape of resource contention during simultaneous JVM initialisation,
not of anything specific to any one service's code. It corroborates the
"expensive first request" reframing above with hard numbers: a request arriving
while the JVM is still 100+ seconds from ready will of course look like a
multi-second endpoint.

SCOPE - what this does NOT say
  - Nothing here measures STEADY-STATE latency. GET /accounts still runs at
    ~30-60ms once warm; that finding is unchanged.
  - Warm restarts are believed to be considerably faster, but that was NOT
    measured the same way and should not be quoted as if it were. The only
    supporting observation is that a mid-session `stack.sh up` against warm
    volumes reached all-healthy about 80s after `up` returned - a different
    metric (time-to-healthy, not JVM boot) and only an upper bound. Anyone
    picking this up should measure a warm boot with the same "process running
    for" line before comparing.
  - The cold-start time itself was NOT addressed. The 2026-08-02 session only
    fixed the false "unhealthy" signal it caused: the healthcheck marked a
    container unhealthy at start-period + retries*interval = 90 + 5*15 = 165s,
    which the five business services straddled at 162-173s, so they flipped to
    unhealthy and then recovered on their own. start-period is now 240s. The
    gateway at ~110s had also already exceeded the old 90s start-period and was
    surviving only by needing fewer than 5 retries.

WHAT TO DO NEXT
  - Start with starting point 1 (engine resources). The boot-time numbers above
    now point at it directly rather than by elimination.
  - Capture a WARM boot baseline using the same "process running for" line, so
    the cold/warm delta is measured rather than assumed.
  - Re-measure request latency deliberately: capture first-request-after-startup
    separately from steady state, for each endpoint. If only the first request is
    slow, this is a warm-up characteristic and not a defect. Note the boot-time
    data makes it likely that the original figures were taken before the JVM had
    finished starting at all.
  - A cheap experiment that would isolate contention conclusively: start the five
    business services ONE AT A TIME against an otherwise idle engine and record
    the same boot line. If a lone service boots in ~25-30s like config-server,
    contention is proven and the fix is engine resources or staggered startup,
    not application code.
  - The logging change is kept regardless: it removed redundant and dead
    configuration and reduces log volume, which is worth having on its own terms
    even though it was not the cause.

ORIGINAL PROPOSED FIX (now applied, retained for context):
Lower org.hibernate logging in the config repo's dev profile. Keep it granular
rather than switching Hibernate logging off: org.hibernate.SQL at DEBUG shows the
statement itself without every parameter bind and is cheap and useful. The
expensive parts are the per-parameter TRACE loggers and format_sql.

CONFIRM THE SCALE FIRST, in Kibana:
    logger_name:org.hibernate* AND traceId:"<any recent traceId>"
counts exactly how many log lines one request produces. Worth doing before and
after so the improvement is measured rather than assumed - and it also tells you
whether this fully explains the numbers or only part of them.

Note the interaction with starting point 1 below: heavy log writing and engine
resource contention would compound, so both may be contributing. Fixing the
logging is cheap and reversible, so do it first and re-measure before spending
time on engine tuning.

Investigation starting points:
  1. Docker Desktop resource allocation (CPU and RAM given to the engine).
     *** NOW THE LEADING CANDIDATE, not merely a plausible one - promoted by the
     2026-08-02 cold-start measurements above. ***
     Fourteen containers run at once - eight application plus six infrastructure
     - so contention was always plausible. The boot-time data makes it concrete:
     six components starting simultaneously take 108-173s, while the two that
     start alone earlier in the dependency chain take under 30s, on the same
     startup machinery.

     ENGINE ALLOCATION MEASURED 2026-08-02 (`docker info`):
         NCPU     = 6          host has 12 logical CPUs
         MemTotal = 11.7 GiB   (the 12GB .wslconfig setting)

     ENGINE ALLOCATION 2026-08-05, after the fix:
         NCPU     = 10         host still 12 logical CPUs
         MemTotal = 11.7 GiB   (unchanged)

     Six CPUs, and exactly six components boot simultaneously - gateway plus the
     five business services. That is ONE core per JVM during the phase where JVM
     startup most wants several: JIT compiler threads, parallel GC threads and
     classloading all contend. config-server and discovery boot earlier in the
     dependency chain with all six cores to themselves, and come up in 19-25s.

     The 1:1 core-to-JVM ratio at the exact moment of the slowdown is a
     quantitative fit for the observed 5-7x difference, not just a directional
     one. This is now a specific, testable claim rather than a general suspicion.

     *** RESOLVED 2026-08-05 - CONFIRMED BY EXPERIMENT. ***
     The proposed test was run: .wslconfig raised to 10 CPUs (docker info NCPU=10),
     identical cold start repeated. The six simultaneously-booting components
     dropped 37-47%, while discovery-service - which boots ALONE and was never
     contended - was unchanged at +1.1%. See "RUN 3" in the cold-start section
     above for the full table.

     That control case is what makes it conclusive rather than merely suggestive:
     a slow disk, a cold page cache or slow class loading would have sped up the
     uncontended component too. Only CPU contention leaves the lone booter
     untouched while nearly halving the time of the crowd. The magnitude also
     agrees with the arithmetic - cores per booting JVM went 1.00 -> 1.67, which
     predicts -40%, and -45% was measured.

     REMAINING WORK ON THIS POINT
     Contention was dominant, not total: six JVMs still take ~90s with 1.67 cores
     each. Treat ~90s as the new baseline, not as "solved". If the boot time
     matters further, the next lever is staggering startup so fewer JVMs
     initialise at once - the dependency graph already serialises config-server
     and discovery, and the same could be done to the business tier - rather than
     buying more cores, since the host has only 12 and two should stay free.

     Points 2-4 below are now the open ones; they were previously downstream of
     this.
  2. JAVA_TOOL_OPTIONS="-XX:MaxRAMPercentage=75" from the Step 6 Dockerfile.
     Compare the percentage against each container's actual memory limit and
     look for GC pressure; no per-container memory limits are set in
     docker-compose.yml, so the percentage resolves against whatever the engine
     exposes.
  3. HikariCP maximum-pool-size=5 from Step 4. Unlikely to bottleneck a single
     request, but worth ruling out under concurrent load later.
  4. Overhead from photo-app-tracing-lib (Step 8). Unlikely at this magnitude -
     URLConnectionSender reports asynchronously off the request thread.
     CAVEAT: comparing against a pre-fix trace is probably not possible. Zipkin
     runs with in-memory storage and its container was restarted several times
     during the Step 8 investigation, so no pre-fix traces should survive. A
     cleaner comparison is to run the same endpoints natively, where Step 3
     already showed working tracing, and diff the durations.

Useful extra signal for whoever picks this up: the same endpoints were exercised
natively in Steps 3 and 5 and did not feel slow, which points at the container
environment rather than at application code. Nothing was measured then, so that
is an impression rather than a baseline - capturing a native baseline first would
make the comparison meaningful.
Priority: MEDIUM.


[2026-08-01 | Step 8] Find what interrupts the Zipkin reporter thread
----------------------------------------------------------------------
Follow-up to the FIXED transport-swap item above. The swap restores
observability but only bypasses the defect; the JDK java.net.http.HttpClient is
still broken inside these containers and anything else that uses it would hit
the same wall.

What is established: the reporter fails with ClosedChannelException at
SocketChannelImpl.ensureOpen, i.e. the socket channel is already closed before
connect is attempted. That is the signature of a thread carrying an interrupt
flag, because NIO auto-closes interruptible channels on interrupt and never
reopens them. A freshly built client on a clean thread in the same container
returns 202 for the same request, so the JDK client itself is fine.

HYPOTHESIS FOR THE LOOSE END (why Step 3's native run worked but containers do
not) - NOT CONFIRMED, offered as a starting point:
  Virtual threads. spring.threads.virtual.enabled=true was added in Step 1 and
  may interact differently with NIO channel interruption under container CPU and
  memory constraints than in an unconstrained native JVM. A virtual thread that
  is unmounted, or carrier-thread pinning under CPU pressure, could plausibly
  leave an interrupt flag set on the thread performing the connect.
  Note the setting is currently on the GATEWAY only, while every service loses
  spans - so if virtual threads are the cause, the mechanism cannot be direct
  and that itself is informative. Check whether anything else enables virtual
  threads globally before pursuing this.

Suggested approach:
  - Reproduce with a minimal Boot app in a container, then bisect: with and
    without virtual threads, with and without CPU limits.
  - Instrument AsyncReporter$Flusher to log Thread.currentThread().isInterrupted()
    before each flush, which would confirm or kill the interrupt theory outright.
  - If confirmed, report upstream: this affects any Boot 4 application sending
    spans over the default sender in a container.
Priority: LOW-MEDIUM. Observability works via the URLConnection sender; this is
about the underlying defect, not the symptom.


[2026-08-01 | Step 1] Gateway: backend-unavailable failures render as 500, not 503
---------------------------------------------------------------------------------
When no instance of a downstream service is registered, the load balancer raises
HttpServerErrorException: 503, which propagates as a servlet exception and the
error dispatch renders HTTP 500. The WebFlux stack surfaced 503 directly.
Only affects the service-unavailable error path. A dedicated exception handler at
the gateway would restore fidelity.
Priority: low.


[2026-08-01 | Step 1] Actuator sub-paths shadowed by Spring Cloud Function's web handler
----------------------------------------------------------------------------------------
Spring Cloud Function's web handler arrives transitively via
spring-cloud-starter-bus-amqp and intercepts /actuator/<sub-path> requests,
turning e.g. /actuator/gateway into
"Failed to lookup function to route based on the value of
'spring.cloud.function.definition' property 'actuator/gateway'".
Pre-existing across all five business services as well — not introduced by the
webmvc migration.
CORRECTION 2026-08-01 (Step 4): the proposed fix was WRONG and has been dropped.
spring.cloud.function.web.enabled=false is a no-op here — spring-cloud-function-web
is not on any module's classpath (only spring-cloud-function-context, pulled in by
spring-cloud-stream-binder-rabbit). Measured on the gateway with and without the
flag, results are identical:
    /actuator/gateway         -> 415   (function lookup error, logged once per hit)
    /actuator/gateway/routes  -> 404
    /actuator/health          -> 200
    /actuator/info            -> 200
The original Step 1 reading was a false inference: the flag was changed in the same
sitting as permitting /error and setting management.endpoint.gateway.access, and the
improvement came from those, not from this flag.
Revised severity: LOW / cosmetic. Real actuator endpoints are never shadowed — only
unmapped /actuator/<x> paths fall through to the function handler and return 415
instead of 404. Since /error is permitted (Step 1) this no longer masks anything as
a 401.
Correct fix: unknown, and not worth chasing now. If the gateway actuator endpoint is
ever actually wanted, start by investigating why it 404s under
spring-cloud-gateway-server-webmvc 5.0.1 even with
management.endpoint.gateway.access=unrestricted.
NOT scheduled — deliberately left open.


[2026-08-01 | Step 2] Wrong/missing config-server credentials degrade silently
------------------------------------------------------------------------------
New failure mode introduced by putting HTTP Basic in front of the Config Server.
With spring.config.import=optional:configserver:..., a 401 is logged as a WARN
("Could not locate PropertySource ... 401") and startup CONTINUES with no remote
configuration at all. Whether the app then fails depends entirely on whether some
bean happens to need a property only the server provides — the gateway does fail,
because JwtClaimsParser requires ${photoapp.jwt.secret}, but that is luck, not a
guard. A component whose remote properties are all optional would boot silently
misconfigured.
Fix: drop the "optional:" prefix so a 401 or an unreachable server is fatal.
Already planned for containers via SPRING_CONFIG_IMPORT in Step 8; consider doing
the same for the packaged properties so native runs fail loudly too.
Priority: medium.


[2026-08-01 | Step 2] Config Server health is DOWN whenever RabbitMQ is down
----------------------------------------------------------------------------
/actuator/health is deliberately left unauthenticated for container healthchecks,
but it aggregates the Rabbit health indicator (Spring Cloud Bus), so it returns
503 DOWN whenever RabbitMQ is unavailable — confirmed during the Step 2 smoke
test. A compose HEALTHCHECK on that endpoint therefore reports the Config Server
unhealthy on any Rabbit blip, and ordering must gate the Config Server on
rabbitmq being service_healthy.
Consider a dedicated health group for the readiness probe that excludes rabbit.
Relevant to: Step 7 (compose infrastructure).
Priority: medium — decide before writing the healthchecks.


[2026-08-01 | Step 3] Config Server's Eureka properties are dead config   [RESOLVED]
------------------------------------------------------------------------------------
RESOLVED 2026-08-01: both properties deleted. Decision — the Config Server is
deliberately NOT registered with Eureka: every client reaches it by direct URL
through spring.config.import, so registering it would only introduce a bootstrap
chicken-and-egg problem for no benefit, since nothing needs to discover it
dynamically. EUREKA_URL is therefore not required by this container and must not
be added to its Step 8 compose environment. Verified by starting the Config
Server with EUREKA_URL unset: clean start, no unresolved placeholder.
Original finding below.

Its application.properties declares
  eureka.client.service-url.defaultZone=${EUREKA_URL}
  eureka.instance.prefer-ip-address=true
but the pom has NO eureka client dependency (grep for "eureka" in it returns
nothing), so the Config Server never registers. Confirmed during the Step 3 run:
Eureka listed six registrations — the five business services plus the gateway —
and no configuration server.
This is harmless in practice because clients reach the Config Server by URL via
spring.config.import, not through discovery. But it means EUREKA_URL is a
required-looking env var that does nothing.
Decide: either add spring-cloud-starter-netflix-eureka-client and register it, or
delete both properties and drop EUREKA_URL from the Step 8 compose environment
for this container. Do not leave it as-is.
Priority: low, but resolve before Step 8 writes the compose env block.


[2026-08-01 | Step 6] Runtime images are ~1 GB, mostly base image
-----------------------------------------------------------------
The eight images land at 914 MB - 1.06 GB. Almost all of it is the base:
amazoncorretto:25 alone is 803 MB, so application layers are only ~150-250 MB.
Slimmer bases exist and were confirmed available: amazoncorretto:25-alpine and
amazoncorretto:25-headless. A jlink-produced custom runtime would be smaller
still.
Not changed now because the runtime base was specified as amazoncorretto:25, and
switching to alpine changes the libc (musl vs glibc), which deserves its own
verification pass rather than being slipped into the Dockerfile step.
Relevant to: image pull times in CI and any future ECR/ECS work.
Priority: low for local Docker, medium before the AWS phase.


[2026-08-01 | Step 5] Audit remaining DTO/entity name mismatches
----------------------------------------------------------------
The MapStruct migration exposed that AccountDTO.accountTypeDTO never received
Account.accountType under ModelMapper — the API returned null while the database
held PREMIUM. Fixed by an explicit @Mapping.
The naming convention that caused it is still in place: AccountDTO and
CreateAccountInputDTO use the suffixed name "accountTypeDTO" for a field the
entity calls "accountType". Every other DTO field matches its entity field name
exactly, so this is the only current instance, and MapStruct's
unmappedTargetPolicy=ERROR now makes any recurrence a build failure rather than a
silent null.
Consider renaming AccountDTO.accountTypeDTO -> accountType and
CreateAccountInputDTO.accountTypeDTO -> accountType for consistency with every
other DTO. NOTE: this is an API-breaking change, since the field name appears in
request and response JSON. Not done for that reason.
Priority: low. Decide alongside any future API versioning work.


[2026-08-01 | Step 3] Redundant tracing dependency in the five services   [RESOLVED]
-----------------------------------------------------------------------------------
RESOLVED 2026-08-01: io.micrometer:micrometer-tracing-bridge-brave removed from
all five service poms. spring-boot-starter-zipkin provides it transitively —
confirmed by dependency:tree, which now shows the bridge arriving solely through
that starter. All eight components declare the same two Boot artifacts.
Verified at runtime against a freshly restarted (emptied) Zipkin: the gateway,
discovery service, config server, users service and albums service all reported
spans after the dependency was removed.
Original finding: each service declared both spring-boot-micrometer-tracing-brave
and the explicit bridge, leaving the five services inconsistent with the three
infrastructure components after Step 3.


[2026-08-02 | Step 9] Dead spring.flyway.* properties in a prod config file   [RESOLVED]
---------------------------------------------------------------------------------------
RESOLVED 2026-08-02: all four properties deleted from
photo-app-users-service-prod.properties (config repo commit 50b7f6d).
Re-verified at removal time: no other file in the config repo mentions Flyway,
and no pom in the reactor declares it as a dependency.
NOTE: the original entry below said "five Flyway properties" and then listed
four. Four is correct — that was a miscount in this entry, not a missed line.
Original finding: photo-app-users-service-prod.properties carried
  spring.flyway.enabled=true
  spring.flyway.clean-disabled=true
  spring.flyway.validate-on-migrate=true
  spring.flyway.locations=classpath:db/migration
They were inert — Flyway is not a dependency of any module in the reactor, and
there is no classpath:db/migration directory — so nothing read them.
They were nonetheless wrong: schema migration is Liquibase, centralized in the
database/ module, and Boot 4 would have attempted to auto-configure Flyway the
moment the dependency ever appeared. They also contradicted the rule stated in
docs/DATABASE.md.


[2026-08-02 | Step 9] Authorization service prod logging contradicts the INFO floor   [RESOLVED]
-----------------------------------------------------------------------------------------------
RESOLVED 2026-08-02: both loggers set to info in
photo-app-authorization-service-prod.properties (config repo commit 50b7f6d).
All five business services now agree in prod, and the prod profile matches the
INFO-floor principle stated in docs/LOGGING.md.
Original finding: photo-app-authorization-service-prod.properties set
  logging.level.org.hibernate.SQL=debug
  logging.level.org.hibernate.orm.jdbc.bind=trace
while the other four business services set both to info in prod. The bind logger
at TRACE is the expensive one — it logs every JDBC parameter on every statement,
and it is the same setting that was measured as a latency contributor in dev.
On an auth service that meant every credential lookup had its bind parameters
written to disk and shipped to Elasticsearch.


[2026-08-02 | Step 9] OpenAPI advertised the internal container URL   [FIXED]
------------------------------------------------------------------------------
FIXED 2026-08-02 (config repo fb329e4, corrected in 8f3ee25).

SYMPTOM
In the aggregated Swagger UI at http://localhost:8080/swagger-ui.html, the
Servers field showed each service's INTERNAL container address. "Try it out"
then failed at the fetch: the browser cannot resolve a Docker-network hostname,
and the target was a different origin from the UI page. The Servers dropdown is
not editable in this setup, so there was no manual workaround.

BEFORE / AFTER - servers[0].url as served through the gateway
  before:  http://photo-app-authorization-service:8085
           http://photo-app-users-service:8081
           http://photo-app-accounts-service:8082
           http://photo-app-albums-service:8083
           http://photo-app-photos-service:8084
  after:   http://localhost:8080   (all five)

Fetched directly inside a container the same spec always said
http://localhost:8085, which localised the cause to the proxy hop rather than to
springdoc configuration.

CAUSE
springdoc derives servers[0].url from the incoming request. The gateway rewrites
Host to the lb:// target when it proxies, so each service saw its own internal
address as the request Host and advertised that.

FIX - two properties, and the gateway half is the non-obvious one
  1. gateway:  spring.cloud.gateway.server.webmvc.trusted-proxies=<regex>
     Gateway MVC does NOT send X-Forwarded-* by default. The
     xForwardedRequestHeadersFilter bean is
     @Conditional(TrustedProxies.XForwardedTrustedProxiesCondition) and its
     factory asserts "trustedProxies must not be null", so with the property
     unset no filter exists and no headers are emitted. Setting only the service
     property would have been a complete no-op.
     Value is RFC1918 plus loopback - a DEV value. In production it must be
     narrowed to the real load balancer's address, or a client can spoof its
     apparent origin by sending its own X-Forwarded-Host.
  2. five business services:  server.forward-headers-strategy=framework
     Installs Spring's ForwardedHeaderFilter, which rebuilds scheme/host/port
     from those headers before springdoc reads the request.

TRAP WORTH REMEMBERING - the property is forward-headers-strategy, NOT
forwarded-headers-strategy. The first attempt used the "forwarded" spelling.
Spring binds unknown properties without complaint, so it appeared in
/actuator/env with a value and looked applied, while no ForwardedHeaderFilter
bean was ever created and nothing changed. Confirmed against Boot's own metadata
in spring-boot-web-server-4.0.6.jar. When a property seems ignored, check
/actuator/beans for the bean it is supposed to create, not /actuator/env for the
property - env proves only that the string was read, never that anything uses it.

VERIFIED
  - servers[0].url is http://localhost:8080 for all five services through the
    gateway; ForwardedHeaderFilter present in /actuator/beans.
  - The exact call Swagger UI makes - POST /auth/login, same-origin, with
    Origin and Referer set to the UI page - returns HTTP 200 carrying
    accessToken, refreshToken, tokenType, expiresIn, refreshTokenExpiresIn.
  - CORS is now moot rather than merely permitted: the UI page and the target
    are the same origin (http://localhost:8080), so no preflight is issued at
    all. The earlier failure was cross-origin AND unresolvable.
  - VERIFIED IN A REAL BROWSER 2026-08-02, clicked through end to end. Opened
    http://localhost:8080/swagger-ui.html, selected authorization-service from
    the definitions dropdown, and the Servers field rendered
    "http://localhost:8080 - Generated server url". Try it out on POST
    /auth/login with admin/generic, then Execute: Request URL
    http://localhost:8080/auth/login, Code 200, response body carrying
    accessToken, refreshToken, tokenType Bearer, expiresIn 86400,
    refreshTokenExpiresIn 2592000. No CORS or fetch error. The earlier curl
    reproduction agreed with the real UI on every point.
    (accounts-service, which the UI loads first by default, also rendered
    http://localhost:8080 - so the fix is not specific to one definition.)

INCIDENTAL EVIDENCE FOR THE PKCE ITEM
The same login was executed twice through the UI: once with a body of
{"username","password"} only, and once with a codeVerifier added
("YAL9KbrRgGYyV12_YxRVnV8z54xhM2o8XpbMB1a01Ro"). BOTH returned 200 with valid
tokens. That is direct behavioural confirmation of what the PKCE item above
argues from code: codeVerifier is accepted, ignored and never validated - it is
neither required when absent nor checked when present.

NOTE ON APPLYING IT
Both properties live in the config repo, so no image rebuild is needed - a
container restart is enough. The five business services were restarted and the
three that returned 500 immediately afterwards were transient: Eureka
registration and the load-balancer cache had not caught up. A retry seconds
later returned 200 from all five.


[2026-08-05 | testing-plan] POST /auth/refresh cannot work without an access token   [FIXED]
-------------------------------------------------------------------------------------------
FIXED 2026-08-05. The refresh flow now re-identifies the user from the username
stored on its own refresh-token record and re-verifies through the PUBLIC
GET /users/username/{username} - the same lookup login uses - so it needs no
caller credential and no service-to-service credential. Verified: refresh with no
Authorization header returns 200 with a working access token.
The misleading-503 half was fixed too, in two places: DownstreamFailurePredicate
stops a downstream 4xx counting toward the circuit-breaker failure rate or being
retried, and FeignFallbacks.translate preserves a downstream 4xx instead of
replacing it with a blanket 503. See the 2026-08-05 commit for detail.

Original finding:
Found incidentally while smoke-testing the Clock refactor. PRE-EXISTING - not
caused by that change, and reproduced against code that predates it.

SYMPTOM
  POST /auth/refresh with only {"refreshToken": "..."}     -> 503
  POST /auth/refresh with the SAME body plus a valid
    Authorization: Bearer <accessToken> header             -> 200

CAUSE
TokenHandlerServiceImpl.refreshToken calls
userFeignClient.findById(...) -> GET /users/{id}, which is
@PreAuthorize("hasRole('ADMIN') or hasRole('USER')"). FeignAuthInterceptor
propagates the INBOUND Authorization header, and /auth/refresh is a public
endpoint that a client would normally call WITHOUT one - because the whole point
of a refresh token is to get a new access token when the old one is gone or
expired. With no header to forward the downstream call arrives anonymous,
users-service answers 401, CustomFeignErrorDecoder turns that into
ApplicationException(401), the circuit breaker counts it as a failure, retries
exhaust, and the fallback reports 503 "photo-app-users-service is not available".

The 503 is actively misleading: users-service is healthy and the failure is an
authorization problem, not availability. That is a second-order finding worth
noting on its own - a fallback that reports "service unavailable" for a 401
hides the real cause.

CONFIRMED PRE-EXISTING
Verified against the running stack. None of the 2026-08-05 changes touch Feign
auth propagation or the security chain; the only edit to TokenHandlerServiceImpl
was System.currentTimeMillis() -> clock.millis(), and the with-header call
returns 200 with the expected expiresIn=86400, so the Clock path itself is fine.

POSSIBLE FIXES (decide, do not assume)
  - Have the refresh flow call users-service with a service-to-service
    credential rather than relying on the caller's token.
  - Or drop the findById call: the refresh token record already carries the
    userId, so the active-user re-check is the only thing needing it - and that
    check could move to a dedicated internal endpoint.
  - Either way, teach CustomFeignErrorDecoder/the fallbacks to distinguish
    "downstream rejected us" from "downstream is down", so a 401 does not
    surface as a 503.
Priority: HIGH - the refresh flow is unusable as designed for its actual use
case, and the whole point of Phase 4 of testing-plan.md is catching exactly this.


[2026-08-05 | testing-plan Phase 1] HIGH — type-mismatch and no-handler both return 500
----------------------------------------------------------------------------------------
Found by the Phase 1 control test, then confirmed against the running stack. This is the
Step 8 bug class again, and at far greater scale than the three defects fixed earlier
the same day: it affects essentially every endpoint taking a typed parameter.

GlobalExceptionHandler has no handler for Spring MVC's own request-binding failures, so
they all fall through to @ExceptionHandler(Exception.class) and are reported as
500 "Unexpected error occurred". Three distinct sub-classes, all measured at 500:

  A. Non-numeric path variable on an existing {id} endpoint -> should be 400
       GET /users/abc        500
       GET /accounts/abc     500
       GET /albums/abc       500
       GET /photos/abc       500
     Log: UNHANDLED_EXCEPTION ... Method parameter 'id': Failed to convert value of type
     'java.lang.String' ... i.e. MethodArgumentTypeMismatchException.
     Roughly 20 endpoints across the five services take @PathVariable Long id.

  B. Genuinely unmapped path -> should be 404
       GET /users/x/y/z      500
       GET /accounts/a/b/c   500
     NoResourceFoundException is a ServletException subclass and is swallowed the same way.
     Every service therefore answers 500 for a typo in a URL.

  C. Un-convertible query parameter -> should be 400
       PATCH /users/1/activate?activate=maybe          500
       PATCH /accounts/1/type?accountTypeDTO=GOLD      500
     Bad boolean and bad enum respectively.

WHY IT MATTERS BEYOND THE STATUS CODE
Each of these logs at ERROR with the UNHANDLED_EXCEPTION marker and a full stack trace, so
ordinary client mistakes are indistinguishable from genuine server faults in Kibana. The
marker that should mean "something is broken" currently fires for a mistyped URL.

FIX (Phase 2 of testing-plan.md - deliberately not done in Phase 1, which is infrastructure)
Add handlers to GlobalExceptionHandler, ABOVE the catch-all, for:
  - MethodArgumentTypeMismatchException  -> 400, naming the parameter and expected type
  - NoResourceFoundException             -> 404
  - HttpMessageNotReadableException      -> 400 (malformed JSON body; not yet measured,
                                                 but the same mechanism applies)
  - HttpRequestMethodNotSupportedException -> 405 (likewise unmeasured)
Then extend the Phase 2 suite to cover each, since the handler ORDER is what the whole
Step 8 lesson was about.
Priority: HIGH. Reachable by any client on any typed endpoint, and it pollutes the error
signal that the observability stack depends on.


  *** RESOLVED 2026-08-06 — testing-plan Phase 2. ***
  All four handlers added and covered. Re-measured against the running stack after
  rebuilding the five service images:

    GET /users/abc                            500 -> 400  TYPE_MISMATCH @WARN
    GET /users/1/does-not-exist               500 -> 404  RESOURCE_NOT_FOUND @WARN
    PATCH /users/1/activate?activate=maybe    500 -> 400  TYPE_MISMATCH @WARN
    POST /users  body {"broken                500 -> 400  MALFORMED_REQUEST_BODY @WARN
    DELETE /auth/login                        500 -> 405  METHOD_NOT_ALLOWED @WARN

  Sub-classes D (malformed body) and E (wrong method) were listed above as "not yet
  measured" — both were measured at 500 before the fix, confirming the mechanism.

  The observability half is fixed too: zero events at level ERROR across users-service and
  authorization-service for the whole sequence, where previously each produced an
  UNHANDLED_EXCEPTION at ERROR with a full stack trace.

  Regression-checked on the same stack: 403 for a ROLE_USER token on ADMIN-only GET /users
  (the Step 8 case), 200 on the happy path, 404 via ApplicationException, and 400 from
  PaginationUtil all unchanged.


[2026-08-06 | testing-plan Phase 2] LOW — 405 responses omit the Allow header
----------------------------------------------------------------------------------------
methodNotSupportedHandler returns 405 through the shared buildResponse, which sets no
headers. RFC 9110 §15.5.6 requires a 405 response to carry an Allow header listing the
methods the resource does support; HttpRequestMethodNotSupportedException already exposes
them via getSupportedHttpMethods().

Not done in Phase 2 because buildResponse is shared by all eleven handlers and adding a
per-handler header would mean either a second builder or an optional-headers parameter
threaded through every call site. Deferred deliberately rather than gold-plating the
handler that was being fixed.

Priority: LOW. Correctness-of-protocol issue, not a functional one — no client in this
system reads Allow, and the status and body are already correct.


[2026-08-06 | testing-plan Phase 3] HIGH — an expired token locks a client out of /auth
*** FIXED 2026-08-07 — see the resolution block at the end of this entry. ***
----------------------------------------------------------------------------------------
All three /auth endpoints returned 401 when the caller sent an EXPIRED token, even though
/auth/** is permitAll. Found by AuthorizationControllerAuthorizationTest, which pinned the
behaviour as a characterization test until the fix landed; that test is now the regression
guard (expiredTokensDoNotBlockAnyAuthEndpoint).

Mechanism. JwtFilter runs ahead of the authorization rules and treats ExpiredJwtException
differently from every other failure:

    catch (ExpiredJwtException e)  -> sendError(401) and RETURN, chain never continues
    catch (Exception e)            -> clear context, fall through, chain continues

Because the expired branch returns without calling filterChain.doFilter, the permitAll rule
for /auth/** is never consulted. That is why a malformed token does NOT block login but an
expired one does.

Why it matters. /auth/refresh is the damaging case. Its own contract, written on
TokenHandlerService#refreshToken, says:

    "MUST work with no Authorization header. A client refreshes precisely because its
     access token is missing or expired, so requiring one defeats the endpoint."

The filter defeats it whenever the client also SENDS the expired token - the default
behaviour of essentially every HTTP client interceptor, which attaches the stored token to
every outgoing request. The user is then wedged: refresh 401s, and the login they would
retry instead 401s for the same reason. Recovery requires clearing local storage by hand.

Severity is HIGH on user impact and LOW on security - nothing is wrongly ADMITTED, the
failure is over-rejection. There is no data exposure.

Likely fix. Treat expiry like every other invalid token: clear the context, log at WARN,
and continue the chain, letting authorizeHttpRequests decide. Protected paths then still
401 (via the entry point) while permitted paths proceed. The current sendError also
bypasses GlobalExceptionHandler, so the 401 body is a container error page rather than
ApiErrorDTO - worth fixing in the same change.

Not attempted here: Phase 3's scope is asserting the authorization boundary, not changing
the filter, and this deserves its own before/after measurement against the running stack.

  *** CONFIRMED AGAINST THE LIVE STACK 2026-08-07. ***

  Method. Registered a probe user through the public POST /users (id 101, phase3probe) and
  logged in for a real refresh token, rather than guessing a seeded password. Minted the
  expired access token with the same TestJwt fixture the characterization test uses, so curl
  sent exactly what the test asserts against. Control first: a VALID minted token returned
  200 on GET /users/101, proving TestJwt.SECRET is the production secret and the expired
  token is therefore genuinely production-signed, not merely malformed.

  The paired result, same refresh token, same second, ONLY the header differing:

    POST /auth/refresh  + expired Authorization header   -> 401
    POST /auth/refresh  + no Authorization header        -> 200, new access token issued

  The 401 came first, so it left the refresh token unconsumed - the 200 that followed on the
  same token proves the credential was valid throughout and the header was the only cause.

  All three endpoints, each paired against its own no-header control:

    POST /auth/login    expired -> 401   |  no header -> 200
    POST /auth/refresh  expired -> 401   |  no header -> 200
    POST /auth/revoke   expired -> 401   |  no header -> 200

  Two details the slice test could not show:

  1. The GATEWAY rejects, not the authorization service. All three JwtFilter WARNs are
     logged by service=photo-app-api-gateway, and the authorization service logged only the
     three control requests - the rejected ones never reached it. The fix belongs in
     JwtFilter (shared), but the blast radius is the edge.

  2. The 401 body is Spring's default error shape, not ApiErrorDTO:

       {"timestamp":"2026-08-07T04:30:56.794Z","status":401,
        "error":"Unauthorized","path":"/auth/refresh"}

     confirming sendError bypasses GlobalExceptionHandler entirely.

  Logging is at WARN, not ERROR, so the observability half is already correct - unlike the
  Phase 2 defects, this one does not pollute the error signal. Only the status and the
  short-circuit are wrong.


  *** RESOLVED 2026-08-07. ***

  The fix, in JwtFilter.doFilterInternal - four lines removed, no new ones:

    catch (ExpiredJwtException e) {
        SecurityContextHolder.clearContext();
        log.warn("Expired JWT detected ...");
    -   response.sendError(HttpServletResponse.SC_UNAUTHORIZED, "Token expired");
    -   return;
    }

  Expiry is now handled exactly like every other token failure: clear the context, log, and
  continue the chain. The filter's job is to establish an identity when one is presented, not
  to decide what an anonymous request may reach - authorizeHttpRequests decides that, and it
  runs after this filter. Deliberately narrow: no change to token lifetimes, refresh
  semantics, rotation or storage. Those are gathered in jwt-token-requirements.md.

  Re-measured against the rebuilt stack (gateway, authorization- and users-service images
  rebuilt from the fixed security-lib):

    permitAll, WITH an expired token           before   after
      POST /auth/login                           401      200
      POST /auth/refresh                         401      200
      POST /auth/revoke                          401      200

    protected paths - must NOT have loosened   before   after
      GET /users/101   expired token             401      401
      GET /users/101   no token                  401      401
      GET /users/101   valid token               200      200
      GET /users       valid ROLE_USER token     403      403   (Step 8 guard)
      GET /accounts    expired token             401      401
      GET /albums      expired token             401      401
      GET /photos      expired token             401      401

  The protected half is the half worth stating: the whole risk of this change was loosening
  something. Nothing loosened. Protected paths still reject an expired token with 401; they
  now reach it through authorizeHttpRequests and the authenticationEntryPoint rather than by
  the filter short-circuiting, which is the mechanism that was supposed to decide it all along.

  Still logged at WARN by JwtFilter, and zero ERROR-level events across the three services
  for the whole verification sequence. (Two ERRORs appeared during the restart itself - a
  Feign 503 from the authorization service while users-service had not yet registered with
  Eureka - unrelated to this change and absent from every post-warmup run.)

  Full reactor: 292 tests, BUILD SUCCESS.

  ---- Adjacent, NOT fixed here ----

  The generic catch in the same method still logs at ERROR:

      catch (Exception e) { ... log.error("Invalid JWT for request path={} ...") }

  A malformed or forged token is a CLIENT error, and Phase 2 established the convention that
  those log at WARN with their own marker so genuine faults stay visible in Kibana. This means
  anyone spraying junk tokens at the gateway can generate ERROR-level noise at will. Left
  alone because the brief for this fix was explicitly narrow; it is a one-word change and
  worth doing next time this file is open.

  Priority: LOW. Observability only - the status is already correct (401 via the normal
  authorization path), and no request is wrongly admitted or refused.


[2026-08-07 | testing-plan Phase 4] MEDIUM — PATCH is unreachable over Feign's default
client, so all three activateOrDeactivate methods cannot issue a request at all
                                                          *** FIXED 2026-08-07 ***
----------------------------------------------------------------------------------------
Found by Phase 4's client suites, fixed the same day. Resolution at the end of this
entry.

SYMPTOM
  AccountFeignClient.activateOrDeactivate   PATCH /accounts/{id}/activate
  AlbumFeignClient.activateOrDeactivate     PATCH /albums/{id}/activate
  PhotoFeignClient.activateOrDeactivate     PATCH /photos/{id}/activate

All three throw before a socket is opened:

  feign.RetryableException: Invalid HTTP method: PATCH
    executing PATCH http://photo-app-accounts-service/accounts/7/activate?activate=false
  Caused by: java.net.ProtocolException: Invalid HTTP method: PATCH
    at java.net.HttpURLConnection.setRequestMethod(HttpURLConnection.java:453)
    at feign.Client$Default.convertAndSend(Client.java:176)

The downstream never receives anything - asserted in the characterization test by
checking WireMock's request journal is empty, which is what distinguishes "the
request was refused" from "the request was never sent". So there is no downstream
log line either; the failure is invisible on the callee's side.

CAUSE
java.net.HttpURLConnection validates the verb against a fixed whitelist that has
never included PATCH. Feign uses it because feign.Client$Default is the fallback
transport when nothing better is on the classpath.

CONFIRMED TO BE PRODUCTION, NOT A TEST ARTEFACT (checked 2026-08-07)
Spring Cloud only swaps the transport when feign.hc5.ApacheHttp5Client is present;
plain httpclient5 does NOT satisfy HttpClient5FeignLoadBalancerConfiguration's
@ConditionalOnClass. Verified inside all five running service images:
  - feign-hc5           absent from all five (grep -c returned 0 for each)
  - okhttp / feign-java11 / feign-http2   absent
  - no module anywhere defines a feign.Client bean
  - no spring.cloud.openfeign.httpclient.* property in the config repo
httpclient5-5.5.2.jar IS in the images, but it arrives via other dependencies and
is not the artifact the condition looks for. So production runs Client$Default.

The feign-lib test module therefore deliberately does NOT put a PATCH-capable
transport on its test classpath - a comment in its pom.xml says so. Adding one
would make these three methods pass in the suite and still fail in production.

WHY IT HAS NEVER BEEN SEEN
Nothing calls any of the three. They are three of the eleven Feign methods with no
call site anywhere in the reactor (see the Phase 4 inventory in testing-plan.md).
The first caller would get a 500 that looks like a downstream fault and is not one.

Note the SERVICES' own PATCH endpoints are fine - a normal HTTP client reaching
PATCH /accounts/{id}/activate through the gateway works. Only the service-to-service
Feign hop is affected.

OPTIONS (decide, do not assume)
  - Add io.github.openfeign:feign-hc5 as a dependency of photo-app-feign-lib. Fixes
    all three at once and gains a connection pool, but moves EVERY Feign call in all
    five services onto a new transport - a real blast radius for a latent bug in
    three uncalled methods. Would also change the retry-amplification behaviour in
    the item below.
  - Or change the three methods to @PutMapping to match what the transport supports,
    and add the matching @PutMapping to the three controllers. Smaller, but leaves a
    verb chosen to work around a client limitation.
  - Or delete all three methods, since nothing calls them.

Priority: MEDIUM. Nothing is broken today because nothing calls them; this is a trap
laid for whoever adds the first caller.

  ---- RESOLVED 2026-08-07 ----

  io.github.openfeign:feign-hc5 added to all five business services' poms, plus
  photo-app-feign-lib as <optional>true</optional> so the library can compile the
  auto-configuration below without forcing a transport on its consumers.

  No bean and no property selects the transport: Spring Cloud OpenFeign's
  HttpClient5FeignLoadBalancerConfiguration is @ConditionalOnClass(ApacheHttp5Client),
  so the artifact's presence is the whole mechanism. Verified in every running
  container after the rebuild - the beans httpClient5 and hc5ConnectionManager are
  present in all five, read from /actuator/beans:

      users  accounts  albums  photos  authorization    ->  httpClient5 present in all 5

  BEFORE / AFTER, PatchVerbTest (real socket, real transport, WireMock downstream):

      PATCH /accounts/{id}/activate    ProtocolException ->  200, request delivered
      PATCH /albums/{id}/activate      ProtocolException ->  200, request delivered
      PATCH /photos/{id}/activate      ProtocolException ->  200, request delivered
      WireMock request journal         EMPTY             ->  1 request each, correct
                                                             verb and query param

  The characterization test PatchVerbCharacterizationTest became PatchVerbTest and
  now asserts the opposite. It is the ONLY thing that would notice feign-hc5 being
  dropped from a pom, since the transport is selected silently by classpath presence
  and nothing fails loudly when it is absent.

  NOT verifiable through the live stack, and the reason is the defect itself: no
  service calls any of the three Feign activateOrDeactivate methods, so there is no
  request path that exercises the Feign PATCH hop end to end. The services' OWN
  PATCH endpoints were never affected - PATCH /accounts/201/activate through the
  gateway returned 200 both before and after, because that hop is plain HTTP and not
  Feign. Proof for the Feign hop is PatchVerbTest, which uses the identical
  transport, the identical @FeignClient interfaces and a real socket.


[2026-08-07 | testing-plan Phase 4] LOW — a transport failure reaches the downstream
six times, not the three that maxAttempts says
                                                          *** FIXED 2026-08-07 ***
----------------------------------------------------------------------------------------
Found by Phase 4's retry suite, fixed the same day - but NOT by the change that was
expected to fix it. Resolution at the end of this entry, including a correction to
the diagnosis below.

MEASURED
One logical Feign call against a downstream that resets the connection delivers SIX
requests. Asserted in RetryBehaviourTest against WireMock's request journal.

  resilience4j maxAttempts=3        x  HttpURLConnection's own retry-once  =  6

java.net.HttpURLConnection silently retries an idempotent request once when the
connection fails mid-flight. Resilience4j knows nothing about it, so neither its
metrics nor the maxAttempts=3 setting in the config repo says the real number
anywhere.

NOT UNIFORM - and the asymmetry is the awkward part
A decoded 5xx RESPONSE is attempted exactly three times (also asserted). The
doubling happens only when the connection itself breaks - which is precisely the
"downstream is unreachable or overwhelmed" case, where double the traffic is least
affordable. With production's 2s->4s backoff the six attempts also stretch the
caller's wait.

Fixed automatically if the feign-hc5 option in the item above is taken, since
HttpClient5 does not do this. Worth deciding the two together.

Priority: LOW. Correctness is unaffected; this is load amplification on an already
failing downstream, at the current traffic level of one developer.

  ---- CORRECTION TO THE ABOVE ----

  "Fixed automatically if the feign-hc5 option is taken, since HttpClient5 does not
  do this" was WRONG. Measured immediately after adding feign-hc5, before anything
  else changed:

      transport failure (connection reset), @Retry maxAttempts=3    ->  6 requests
      downstream 503, on a method with NO @Retry at all             ->  2 requests

  HttpClient 5 has its own DefaultHttpRequestRetryStrategy: maxRetries=1, and its
  default retriableCodes are 429 and 503. So switching transport did not remove the
  amplification, it MOVED it - and widened it, because the old client only retried
  on a broken connection while HC5 also retries 503 RESPONSES. The second line above
  is the sharper statement of the problem: a retry on a method where retrying was
  explicitly not asked for.

  Both transports retry underneath Resilience4j. The number either one picks is not
  the issue; two retry layers that do not know about each other is.

  ---- RESOLVED 2026-08-07 ----

  New auto-configuration in photo-app-feign-lib:

      com.photoapp.feign.configuration.FeignTransportAutoConfiguration
          @ConditionalOnClass(ApacheHttp5Client)
          @Bean HttpClientBuilderCustomizer -> builder.disableAutomaticRetries()

  Registered through META-INF/spring/...AutoConfiguration.imports rather than
  component scan, because none of the five services scans com.photoapp.feign - they
  reach FeignConfiguration through each @FeignClient's configuration attribute
  instead. HttpClientBuilderCustomizer is Spring Cloud OpenFeign's supported hook
  into the CloseableHttpClient it builds, so this ADDS a bean rather than replacing
  the client and inheriting responsibility for the rest of its configuration.

  Resilience4j is now the only retry authority: one place to configure, one set of
  metrics, and DownstreamFailurePredicate actually gets consulted. The transport's
  retries were invisible to all three.

  BEFORE / AFTER, measured in RetryBehaviourTest and PhotoFeignClientTest against
  WireMock's request journal:

      one logical call, downstream unreachable      6  ->  3   (= maxAttempts)
      one logical call, downstream returns 500      3  ->  3   (unchanged)
      one call, 503, method has no @Retry           2  ->  1   (HC5-only regression,
                                                                introduced and
                                                                removed the same day)

  Verified in every running container: the bean noTransportLevelRetries is present in
  all five services, read from /actuator/beans after the rebuild.


[2026-08-07 | testing-plan Phase 4] LOW — a non-standard downstream status escapes
CustomFeignErrorDecoder as IllegalArgumentException
----------------------------------------------------------------------------------------
Found by Phase 4's decoder suite. Characterized, not fixed.

CAUSE
The first statement in decode() is HttpStatus.valueOf(response.status()), which
throws IllegalArgumentException for any code not in the IANA-registered set - before
the switch is reached. An ErrorDecoder is contractually supposed to RETURN the
exception, not throw one.

CONSEQUENCE
A different exception goes up the stack than the fallbacks are written against:
FeignFallbacks.translate finds no ApplicationException in the cause chain, falls to
its default branch and reports 503. DownstreamFailurePredicate does the same and
counts it toward opening the circuit. So a downstream emitting, say, 499 or a
Cloudflare 52x would be reported as an outage regardless of what it meant.

REACHABILITY
Not reachable from this system today - all five services answer with standard codes.
It needs a misbehaving proxy, a load balancer inserting its own status, or a future
third-party downstream. Recorded so the behaviour is known rather than discovered
during an incident.

FIX IF TAKEN
Replace HttpStatus.valueOf(int) with HttpStatus.resolve(int) and handle null in the
default branch, keeping the raw int in the message.

Priority: LOW.
